/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
     "https://be-storydots.onrender.com"
    ], 
  },
};

module.exports = nextConfig;
